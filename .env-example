# Database connection details
DB_USER=your_database_user          # Replace with your SQL Server username
DB_PASSWORD=your_database_password  # Replace with your SQL Server password
DB_SERVER=your_server_address       # Replace with your SQL Server hostname or IP (e.g., localhost or 127.0.0.1)
DB_DATABASE=your_database_name      # Replace with your SQL Server database name

# Additional settings (optional, depending on your environment)
DB_PORT=1433                        # Default port for SQL Server; change if using a non-standard port
